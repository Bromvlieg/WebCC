local ws, err = http.websocket ("ws://aaa.felid.eu:27055/install"); ws.send(textutils.serializeJSON( {cmd = "install", data = os.getComputerLabel()} )); print("aaa"); local code = ws.receive(); ws.close(); local fname = "startup"; if (fs.exists(fname)) then fs.delete(fname); end local file = fs.open(fname, "w"); file.write(code); file.close(); local f, _ = load(code); print(f, _); f();